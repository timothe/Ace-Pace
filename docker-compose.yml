services:
  ace-pace:
    build: .
    image: ace-pace
    container_name: ace-pace
    volumes:
      - /path/to/OnePaceLibrary:/media:rw
    networks:
      - "proxy"
    environment:
      - TRANSMISSION_HOST=192.168.178.45
      - TRANSMISSION_PORT=9091
      - ACEPACE_DOWNLOAD=transmission
      #- ACEPACE_URL=https://nyaa.si/?f=0&c=0_0&q=one+pace+720p&o=asc
      #- ACEPACE_DB=true
      #- TRANSMISSION_USER=admin
      #- TRANSMISSION_PASS=password
networks:
  proxy:
    driver: bridge
